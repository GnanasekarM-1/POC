import React from "react";
import ResizableLayoutControlled from "components/ResizableLayoutControlled";

const Dashboard = props => <ResizableLayoutControlled {...props} />;

export default Dashboard;
